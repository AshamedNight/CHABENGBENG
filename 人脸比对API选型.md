# 风险自查·人脸比对 API 选型报告

> 结论先行：**推荐腾讯云人脸识别（人脸比对 CompareFace 逐张比对起步，规模化后迁移人脸搜索）**。价格可忽略，接入快，与微信生态同体系。但**合规上有一个比选型更重要的问题**，见第三节。

## 一、业务场景确认

「风险自查」：用户上传一张照片 → 在材料库已收录的人物照片中找相似对象 → 返回相关对象列表（匹配度/平台/记录数）。

这是典型的 **1:N 人脸搜索**场景，两种技术实现：

| 方案 | 原理 | 每次自查成本 | 维护成本 |
|------|------|-------------|---------|
| A. 逐张 1:1 比对 | 用户照片 vs 库中每张人物照片，循环调 CompareFace | N 次调用（N=库中照片数） | 零维护，材料入库不用额外操作 |
| B. 人脸搜索 1:N | 建人员库，调 SearchFaces 一次返回 Top-N | 1 次调用 | 材料入库/删除要同步维护人员库 |

**建议**：材料库 <1000 张照片时用方案 A（实现最简单，成本可忽略：1000张 × 0.0032元 = 3.2元/次自查，且可对同一材料只取1张代表照片）；超过后迁移方案 B。

## 二、候选对比

| | 腾讯云人脸识别 | 百度AI人脸识别 |
|---|---|---|
| 1:1 比对价格（后付费） | 0.0032 元/次 | 0.004 元/次量级 |
| 免费额度 | 每月 1 万次免费资源包 | 有免费 QPS 额度 |
| 资源包 | 10万次 318 元（活动价 206.7） | 类似 |
| 资质要求 | 实名认证账号可开通在线API | 实名认证 |
| 与小程序契合度 | 同腾讯体系，图片URL可直传 | 需 base64 或公网URL |
| 文档/SDK | Python SDK 成熟 | 成熟 |

两者能力相当，**推荐腾讯云**：比价接口返回 0-100 相似度分（官方建议 ≥70 判同人，误识率千分之一；≥80 误识率万分之一），支持直接传图片 URL（我们材料库照片本来就有 URL），不用搬运 base64。

**接入方式**：密钥只能放后端，小程序端接口 `/api/photo/match` 保持不变，后端内部实现从「随机抽样」替换为真实比对。前端零改动。

## 三、⚠️ 合规红线（比选型更重要）

《人脸识别技术应用安全管理办法》（网信办+公安部，**2025年6月1日已施行**）核心要求：

1. 处理人脸信息需取得**单独同意**，且充分告知处理目的、方式、保存期限
2. 人脸信息保存期限不得超过实现目的所需最短时间
3. 事前需做**个人信息保护影响评估（PIA）**并留记录
4. 人脸信息存储达 **10万人** 起 30 个工作日内须向省级网信部门**备案**

**本项目的高风险点**：材料库里的人物照片来自提交者上传的**他人照片**，被比对者本人从未同意。虽然我们只是"比对"不做识别定性，但只要把他人人脸信息用于比对计算，就属于"处理人脸信息"，缺少单独同意的合法基础。

**降低风险的产品建议**（与你"只做比对"的定位一致）：
- 把「风险自查」严格限定为**本人自查**：用户协议已写明"仅可上传本人照片"，建议产品上加自拍实时拍摄（`camera` 组件，禁选相册）+ 提示语双重约束
- 自查照片**比对完即删**，不落库（满足"最短保存期限"）
- 返回结果只展示"是否匹配到相关记录"，弱化"匹配度打分"的识别色彩
- 上线前做一次 PIA 并留存文档
- 材料库人脸照片规模接近 10 万前就要准备备案（很远期，但要知道有这条线）

## 四、落地清单（等资质）

| 步骤 | 依赖 | 状态 |
|------|------|------|
| 1. 注册腾讯云账号 + 实名认证 | 个人即可（建议与企业主体统一） | 待办 |
| 2. 开通人脸识别服务，拿 SecretId/SecretKey | 步骤1 | 待办 |
| 3. 后端替换 `/api/photo/match` 实现（1:1 逐张方案） | 步骤2，代码我随时可以写 | **代码可先行** |
| 4. 前端改实时拍摄上传 + "仅本人照片"提示 | 无 | 可先做 |
| 5. 自查照片比对后自动删除 | 步骤3 | 随代码交付 |
| 6. PIA 评估文档 | 法务/自己按模板写 | 待办 |

> 注：步骤 3、4、5 的代码不依赖真实密钥就能写（密钥走环境变量，没配时接口返回"功能维护中"），要现在做就说一声。
